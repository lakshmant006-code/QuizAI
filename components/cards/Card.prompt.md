Base surface for all tinted content blocks. `tint="maroon"` for quiz-history content, `tint="gold"` for study-summary content, `tint="white"` for neutral panels. Set `interactive` for hoverable rows.

```jsx
<Card tint="maroon"><StatTile .../></Card>
```
