Brand glyph (Font Awesome `fa-brain`, maroon) and the full "glyph + QuizAI" wordmark lockup used top-left of every screen.

```jsx
<Logo />
<BrainMark size={32} />
```

No official logo file was in source material — flagged as a reconstruction, not an authoritative asset.
