import * as React from "react";

/**
 * Brand mark (Font Awesome brain glyph) and full "brain + QuizAI" lockup. No standalone logo file was provided in source — this is a reconstruction.
 */
export interface BrainMarkProps { size?: number }
export interface LogoProps { size?: number }
