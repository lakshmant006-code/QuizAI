Sticky top bar used on every authenticated screen: brand lockup left, links + user chip right, active link in maroon.

```jsx
<NavBar active="Summaries" userName="Lakshman Thota" userInitials="LT" />
```
